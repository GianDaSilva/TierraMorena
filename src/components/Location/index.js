import React from 'react'
import { GoogleMap, withScriptsjs, withGoogleMap} from 'react-google-maps';

const Map = (props) => {
    return (
        <div>
            
        </div>
    )
}

export default Map;
